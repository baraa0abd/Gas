services:
  - type: web
    name: separatorsizer-pro
    runtime: python
    plan: free
    buildCommand: bash build.sh
    startCommand: gunicorn --chdir backend --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
